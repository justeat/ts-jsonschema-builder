string:
    minLength/maxLength
    pattern
    format
number:
    multipleOf
    minimum, exclusiveMinimum
    maximum, exclusiveMaximum
array
    items
    contains (V6)
    additionalItems
    minItems/maxItems
    uniqueItems
boolean
    enum
null